name: create-label
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-18.04
    steps:
      - uses: actions/checkout@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: 📦 INSTALL NODE LTS
        uses: actions/setup-node@v3
        with:
          node-version: lts/*
      - name: 🚛 CARGANDO DEPENDENCIAS
        run: |
          cd ./extras/createLabels/
          npm init -y
          npm i isomorphic-fetch
      - name: 🏗 CREANDO ETIQUETAS
        run: |
          cd ./extras/createLabels/
          node ./index.js ${{ secrets.GITHUB_TOKEN }} ${{ github.repository_owner }} ${{ github.repository }}
      - name: ❌ BORRANDO SCRIPT
        run: |
            git rm ./extras
            git fetch
            git pull
            git commit -m "feat(bot): Se elimina labelator"
            git push
